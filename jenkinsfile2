pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                // Build Docker image
                script {
                    def customImageName = "jenkins/jenkins:latest"  // Modify this with your image name

                    sh "docker build -t ${customImageName} ."  // Build the Docker image
                }
            }
        }
    }
}
