pipeline {
    agent any

    stages {
        stage('Pull from GitHub') {
            steps {
                // Étape pour effectuer un pull du code depuis GitHub
                git 'https://github.com/votre_utilisateur/votre_projet.git'
            }
        }
    }
}
